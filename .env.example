# Discord bot token from https://discord.com/developers/applications
# (Bot page -> Reset Token / Copy)
DISCORD_TOKEN=

# Everything else (queue role, default results channel) is configured
# in Discord itself with the /setup command after the bot is running.
